#include "main.h"

/**
 * *_strcat - contcatenates (joins) two strings.
 *@dest: array
 *@src: positions
 * Return: dest
 */
char *_strcat(char *dest, char *src)
{
	(void)*dest;
	(void)*src;
}

/**
 * *_strncat - Appends the string pointed to by src
 * to the end of the string pointed to by dest up
 * to n characters long.
 *@dest: This is pointer to the destination array.
 *@src: This is the string to be appended.
 *@n: The maximum number of characters to be appended.
 * Return: dest
 */
char *_strncat(char *dest, char *src, int n)
{
	(void)*dest;
	(void)*src;
	(void)n;
}

/**
 * *_strncpy - Copies the first num characters
 * of src to destination.
 *@dest: Pointer to the destination array.
 *@src: String to be copied.
 *@n: Maximum number of characters to be copied src.
 * Return: dest
 */
char *_strncpy(char *dest, char *src, int n)
{
	(void)*dest;
	(void)*src;
	(void)n;
}

/**
 * *_strcmp - Compares the two strings
 *@s1: String to be compared.
 *@s2: String to be compared.
 * Return: 0 if the two strings are iqual.
 */
int _strcmp(char *s1, char *s2)
{
	(void)*s1;
	(void)*s2;
}

/**
 * *_memset - Copies the character b to the first
 * n characters of the string pointed to.
 *@s: This is a pointer to the block of memory to fill.
 *@b: This is the value to be set.
 *@n: This is the number of bytes to be set to the value.
 * Return: s.
 */
char *_memset(char *s, char b, unsigned int n)
{
	(void)*s;
	(void)b;
	(void)n;
}
